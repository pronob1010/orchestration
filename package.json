{
  "name": "dokancloud-orchestrator-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Local issue worktree orchestration UI for the DokanCloud workspace.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
